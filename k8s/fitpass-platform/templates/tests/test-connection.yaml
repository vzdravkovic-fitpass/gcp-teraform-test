apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fitpass-platform.fullname" . }}-test-connection"
  labels:
    {{- include "fitpass-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fitpass-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
