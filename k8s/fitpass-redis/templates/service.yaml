apiVersion: v1
kind: Service
metadata:
  name: {{ include "fitpass-redis.fullname" . }}
  labels:
    {{- include "fitpass-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "fitpass-redis.selectorLabels" . | nindent 4 }}
